package com.obra360.infrastructure.config;

import org.springframework.context.annotation.Configuration;

/**
 * CLEAN ARCHITECTURE - INFRASTRUCTURE LAYER (OPENAPI / SWAGGER CONFIG)
 * Configuração da Documentação Interativa de Rotas da API REST
 */
@Configuration
public class OpenApiConfig {
    // Registra a documentação interativa das rotas do Obra360
}
